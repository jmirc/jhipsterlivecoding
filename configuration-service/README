# Build the docker image
mvn clean package -Pdocker docker:build

# Run the docker image
docker run -p 8888:8888 -v "{REPLACE_BY_FULL_PATH}/src/main/docker/livecoding-config:/livecoding-config" -e "EUREKA_URL=http://192.168.59.103:8761/eureka/" -t jmirc/configuration-service
